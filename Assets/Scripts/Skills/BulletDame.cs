using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletDame : MonoBehaviour
{
    /*public TurretEnemyModel turretModel;
    public MeleeEnemyModel meleeModel;
    public float bulletDamage;

    private void OnTriggerEnter(Collider other)
    {
        turretModel = other.gameObject.GetComponent<TurretEnemyModel>();
        meleeModel = other.gameObject.GetComponent<MeleeEnemyModel>();
        if (other.gameObject.name.Contains("Turret"))
        {
            turretModel.TakeDamage(bulletDamage);
            Destroy(this.gameObject);
        }
        if (other.gameObject.name.Contains("Melee")) {
            meleeModel.TakeDamage(bulletDamage);
            Destroy(this.gameObject);
        }
    }*/
}
